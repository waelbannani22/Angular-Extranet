import { ExportExcelService } from './export-excel-service';

describe('ExportExcelService', () => {
  it('should create an instance', () => {
    expect(new ExportExcelService()).toBeTruthy();
  });
});
