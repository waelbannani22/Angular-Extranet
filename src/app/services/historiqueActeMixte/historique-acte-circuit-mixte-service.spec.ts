import { HistoriqueActeCircuitMixteService } from './historique-acte-circuit-mixte-service';

describe('HistoriqueActeCircuitMixteService', () => {
  it('should create an instance', () => {
    expect(new HistoriqueActeCircuitMixteService()).toBeTruthy();
  });
});
