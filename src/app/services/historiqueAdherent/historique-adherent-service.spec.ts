import { HistoriqueAdherentService } from './historique-adherent-service';

describe('HistoriqueAdherentService', () => {
  it('should create an instance', () => {
    expect(new HistoriqueAdherentService()).toBeTruthy();
  });
});
