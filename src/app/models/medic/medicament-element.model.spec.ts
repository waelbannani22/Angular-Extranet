import { MedicamentElement } from './medicament-element.model';

describe('MedicamentElement', () => {
  it('should create an instance', () => {
    expect(new MedicamentElement()).toBeTruthy();
  });
});
