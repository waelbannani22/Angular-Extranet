// Contractant
export class Contractant {
    id!: string;
    nom!: string;
    prefixe!: string;
    url!: string;
    numPolice!: string;
    estTiersPayant!: number; // not added to all ; merci de vérifier à chaque fois
   
}
