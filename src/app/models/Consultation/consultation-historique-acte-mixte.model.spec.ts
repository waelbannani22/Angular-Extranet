import { ConsultationHistoriqueActeMixte } from './consultation-historique-acte-mixte.model';

describe('ConsultationHistoriqueActeMixte', () => {
  it('should create an instance', () => {
    expect(new ConsultationHistoriqueActeMixte()).toBeTruthy();
  });
});
